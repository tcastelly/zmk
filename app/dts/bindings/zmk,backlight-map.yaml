description: |
  Allows defining a keymap composed of multiple layers

compatible: "zmk,backlight-map"

properties:
  led-channels:
    type: phandle-array
    required: true
  colors:
    type: uint8-array 
    required: false 